{
  "version": 2,
  "installCommand": "pip install -r requirements-vercel.txt",
  "builds": [
    { "src": "handler.py", "use": "@vercel/python" }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "handler.py" }
  ]
}
