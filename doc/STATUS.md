# 개발 현황 (STATUS)

**기준일**: 2026-07-31 (체결내역 종목별 집계 현재가 표시 + 캐치업 스코프 정리 반영)
이 문서는 "지금 어디까지 왔나"의 저장소 내 공식 기록입니다. 큰 기능이 완료/착수될 때 갱신합니다.

---

## ✅ 완료

| 영역 | 내용 |
|---|---|
| 포트폴리오 · 시세 | KIS(한국투자증권)·키움 연동, KR/US 마켓 스냅샷, 아침 브리핑, US 마켓 리포트 |
| 워치리스트 | 목표 매수/매도가 알림(`target_alerts.py`, dedup 처리), ETF 즐겨찾기·메모·New 태그 |
| FinanceHub | 자산/부채/현금흐름/장부, 결제 커버리지, 백업, 레버리지 분석 패널 |
| 인텔리전스 · 지식 허브 | 뉴스/시그널/캘린더, Gemini 기반 AI 분석 저장→차트 연동, 스터디 리슨/TTS |
| 옥션허브 · 자동매매 | 경매 모듈, 자동매매 모듈, `(stock)` 라우트 그룹 재편 |
| 매매습관(Trade Journal) | `/portfolio/journal` — 매수 근거·확신도·감정·목표/손절가 + 매도 사유·복기, 손절준수·익절타이밍·감정별 승률 자동 계산 |
| 아침 루틴 ↔ 매매습관 연동 | 아침 루틴(Phase4 종목별 계획)에서 세운 목표가·손절가가, 그날 실제 그 종목을 매수하면 매매습관 편집 폼에 "가져오기" 제안으로 뜸(`findStockPlan`, `routineStore.ts`). 목록에도 "📋 아침 계획" 배지 표시 |
| 매매습관 저장 후 스크롤 튐 버그 수정 | 체결 편집 폼 저장 시 목록 전체가 "불러오는 중..."으로 잠깐 대체되며 스크롤이 맨 위로 튀던 문제 — 최초 로드 때만 전체 로딩 표시하도록 변경 + 저장한 행의 화면상 위치를 기준으로 스크롤 보정 |
| 매매습관 — 체결일 전후 주가 흐름 표시 | 편집 폼에 그날 전후 1개월 미니 차트(체결일 강조 표시) + 그날 등락률 + ±5% 이상 급등락일 배지 표시. 백엔드 `/portfolio/stocks/{symbol}/chart`에 `end_date` 파라미터·일별 `change_rate` 추가 |
| 문서화 | `doc/` 통합 인덱스 + GitHub Pages(`stockdashboard-docs`) 발행, 개인 문서 허브 등록, [DEVLOG](DEVLOG.md) 신설, 앱 내 "참고자료" 메뉴(`frontend/public/docs/`, `/docs/index.html`)로도 열람 가능 |
| 시작 시 자동 캐치업 동기화 | 로컬 백엔드 기동 시마다 체결내역(계좌별 마지막 동기화일부터)·잔고·시세를 무조건 한 번 재동기화(`run_startup_catchup_sync`) — 며칠 컴퓨터를 꺼둬도 다시 켜면 갭이 자동으로 메워짐 |
| 장마감 스냅샷 날짜 버그 수정 | `save_daily_snapshot`/`save_portfolio_snapshot`이 `date.today()` 대신 `get_latest_trading_date()`(최근 거래일) 기준으로 저장하도록 수정 + pykrx 전일 데이터 조용한 폴백 제거. 기존에 잘못 찍힌 7/10(7/9 중복)·7/11(실제로는 7/10 실적) 데이터도 직접 복구함 |
| SK하이닉스 ADR 추가 + 국내주 비교 차트 | 나스닥 상장(2026-07-10, when-issued 티커 `SKHYV` → 07-13 `SKHY` 전환 예정) 반영해 US 마켓 리포트 추적 종목에 추가. `TrackedUsStock`에 `compare_krx_symbol` 컬럼 신설 — 클릭 시 국내 000660(SK하이닉스)과 등락률(%) 비교 차트 표시 (`UsTickerChartModal`) |
| 급등락 알림 중복 재발송 버그 수정 | 시세가 임계값(±5%)을 넘은 채로 유지되는 동안 갱신될 때마다(스케줄러·수동 동기화·시작 시 캐치업 등) 새 알림이 계속 쌓여, "전체 읽음" 처리해도 다음 갱신에서 또 안읽음으로 나타나던 문제. `Stock`에 `price_surge_alerted`/`price_drop_alerted` 플래그 추가(목표가 알림과 동일한 엣지 트리거 패턴) — 임계값을 처음 넘을 때 1회만 알리고, 정상 범위로 돌아오면 플래그를 풀어 다음 급등락 때 다시 알림 |
| **KIS 넥스트레이드(NXT) 체결 누락 버그 수정** | KIS 일별체결조회 API가 `EXCG_ID_DVSN_CD` 파라미터를 안 주면 KRX 체결만 반환(기본값)하고 NXT·SOR 체결은 통째로 빠뜨림 — 여기에 더해 `python-kis` 2.1.6 라이브러리가 NXT 거래소코드(`excg_dvsn_cd`)를 몰라서 있어도 KeyError로 조회 자체가 실패. `core/kis_client.py`에 `EXCG_ID_DVSN_CD: "ALL"` 추가 + 미지 거래소코드를 KRX로 관대하게 처리하는 패치 적용. 2024~2026 재동기화로 **344건 누락 체결 복구**(삼성전자 순매수 875→963주, 실제잔고 961주와 -2주 차이로 수렴 / 기아 409→376주, 실제잔고와 정확히 일치) |
| **예측시스템 초기버전 + 매크로/섹터 감성 파이프라인 버그 수정** | `core/ai_analyzer.py`의 분석 프롬프트가 6/2부터 macro/sector 감성 필드를 생략 표기로만 써놔서 AI가 해당 필드를 빼고 반환 → 5주간 매크로 Signal 0건, 섹터 Signal 96%가 기본값 NEUTRAL로 쌓이던 버그 수정(백필은 안 함, 앞으로 데이터부터 정상화). 변동성 스코어(`core/volatility_forecast.py`) 신설로 매수스코어×변동성 매트릭스 완성(`/intel/stocks/{symbol}/forecast`), 이미 계산되지만 프론트에 없던 Signal 적중률·Lead-Lag·리스크레이더를 인텔리전스 허브 "예측·리스크" 탭으로 노출. 이후 매크로/섹터 감성 공백(295건) 재분석 도구도 같은 탭에 추가(개별/7개씩 일괄 처리, 진행률·완료목록·실패사유 표시) |
| **차트 기술적 패턴 적중률 검증(백테스트)** | `한국주식_차트분석_실전가이드.md`(13절 체크리스트)가 이미 `lib/chartAnalysis.ts`에 거의 다 구현돼 있었으나 "지금 조건"만 보여주고 "과거 적중 여부"는 검증 안 하던 것을, MA 골든/데드크로스·볼린저 스퀴즈·거래량 급증 3개 패턴에 한해 이 종목 과거 가격으로 사후 검증하는 `evaluatePatternAccuracy` 추가(AI 호출 없음). 차트에 적중(✓)/실패(✗) 마커, 패널에 스코어카드, 3단계 확인법 판정 문구에도 실측 적중률 반영 |
| **예측시스템 Phase 4 — 패턴 라이브러리** | "금리 인하 발표 같은 매크로 이슈 뒤 특정 섹터가 실제로 어떻게 반응했는지"를 `MacroSignal`×과거 가격으로 집계해 `pattern_library` 테이블에 저장(`core/pattern_library.py`, AI 호출 없음). 새 매크로 Signal이 고신뢰(적중률 60%+, 표본 3건+) 패턴과 일치하면 보유 종목에 알림 자동 생성(`signal_extractor.py` 연동, 중복 방지). 인텔리전스 허브 "예측·리스크" 탭에 패턴 라이브러리 섹션 추가, 매주 일요일 02:00 자동 갱신 |
| **예측시스템 Phase 5 — AI Provider 스코어카드** | GPT/Claude/Gemini 중 어느 AI가 더 정확했는지 Signal 적중률로 비교. `IntelContent`에 실제 사용된 provider를 기록하는 `analysis_provider` 컬럼 신설(기존엔 전혀 기록 안 됐음 — 앞으로 분석분부터 누적), `core/provider_scorecard.py`가 provider별 섹터 Signal 적중률 집계·표본 10건↑ 중 최고 적중 provider 추천. 명시적으로 provider를 지정하지 않는 호출에 한해 자동으로 추천 provider 사용(사용자가 UI에서 직접 고른 경우는 그대로 존중). 인텔리전스 허브 "예측·리스크" 탭에 섹션 추가 |
| **예측시스템 Phase 8 — 섹터 로테이션 감지** | 최근 window_days(기본 30일)를 전·후반기로 나눠 섹터별 `SectorSignal` 평균 감성 점수 변화(delta)를 계산해 상승/하락 섹터 Top3를 뽑고, 보유 종목이 하락 섹터에 편중돼 있으면 경고(`core/sector_rotation.py`, `GET /intel/sector-rotation`, AI 호출 없음). 실측: 2차전지 +0.67→-0.22 급락, 자동차 -0.10→+0.33 반등. 인텔리전스 허브 "예측·리스크" 탭에 섹션 추가 |
| **예측시스템 Phase 7 — 포트폴리오 시나리오 시뮬레이터** | "이 트리거(예: 금리 인하 기대)가 오늘 발생하면 내 포트폴리오는 얼마나 움직일까"를 Phase 4에서 이미 실측한 `pattern_library`의 avg_move_pct·hit_rate를 그대로 재사용해 추정(`core/scenario_simulator.py`, `GET /intel/portfolio/simulate`) — 문서 원안의 하드코딩 시나리오 숫자 대신 실측치 재사용으로 설계 변경, 새 테이블 없음. 종목별 섹터 매칭·비중 가중합산으로 추정 등락률/손익 산출. 인텔리전스 허브에 트리거 선택 드롭다운 + 시뮬레이션 결과 섹션 추가 |
| **예측시스템 Phase 6 — 투자 가설 추적** | "왜 이 종목을 매수했는가"라는 가설을 등록해두면 최근 7일 Signal(매크로/섹터/종목)과 자동 대조해 지지(POSITIVE)/반박(NEGATIVE)을 집계, 표본 3건 이상에서 지지율 80%↑면 confirmed·20%↓면 invalidated로 전환(`investment_theses` 테이블, `core/thesis_tracker.py`, 매일 16:00 스케줄러 + 수동 API). 인텔리전스 허브에 "투자 가설" 탭 신설(생성 폼·상태 필터·만료 처리). 실제 가설 1건("HBM 수요 확대 → 반도체 업황 개선", 삼성전자) 등록·검증 완료(지지 42·반박 9·82% → confirmed) — StockMind 예측시스템 Phase 1~8 로드맵 전체 구현 완료 |
| **노후생활(은퇴 설계) 섹션** | `retire_PRD.md` 기반 v1 — 재정 보드 "노후생활" 탭(`/finance/retirement`). 은퇴 목표 프로필, 4% 룰 필요자금·달성률, 글라이드 패스 목표 주식비중 드리프트, 월 저축 여력(재정보드 등록 + 가계부 3개월 실측), AI 종합 의견(적절성 점수·추세·재정 밸런스 조언·리스크 경고), 주간 리뷰(매주 일 18:00 스케줄러 + 알림) — `core/retirement_service.py`, `api/routes_retirement.py` |
| 허브 내비게이션 개선 | 허브 탭(주식/재정/경매)이 허브별 마지막 방문 페이지를 기억해 복귀 (`hub-navigation.ts`, sessionStorage) |
| 프로그램 전반 점검 (2026-07-12) | 8개 관점 병렬 점검 후 15건 수정 — 오픈뱅킹 토큰 유실·공부하기 본문 공백·고정지출 날짜 밀림·휴장일 스냅샷 등. 상세: [doc/report/2026-07-12-전반점검.md](report/2026-07-12-전반점검.md), 보류 권고 7건은 리포트 4장 |
| 기타 | SpaceX(SPCX) 종목 추가, 포트폴리오/현금흐름 AI 분석 지속 저장 |
| **놓친 시세·수익 이력 캐치업** | 컴퓨터를 꺼둔 사이(장마감 스케줄러가 못 돈 날짜)의 국내 종목 일별 시세를 pykrx 기간 조회로 복원(`catchup_price_history`, 최근 30일 구간을 매번 통째로 재스캔해 중간 구멍도 잡음) + 그 시세를 바탕으로 포트폴리오 일별 손익(`PortfolioSnapshot`)도 복원(`catchup_portfolio_snapshots`, 매매 없었던 종목은 현재 수량으로 정확히·매매 있었던 종목은 체결내역을 되돌려 근사). 앱 시작 시 자동 실행(체결내역 동기화 이후 순서) + 대시보드 "놓친 시세 다시 체크" 수동 버튼(`POST /portfolio/price-history/catchup`). KRX만 대상, 알림은 소급 생성하지 않고 이력만 채움. `PriceHistory` 유니크 제약 추가로 중복 저장 방지, 스냅샷 캐치업은 주말 날짜를 걸러내 기존 오염 데이터를 재생산하지 않도록 함 |
| YouTube 자막 없을 때 Gemini 직접 시청 | 자막이 없는 영상(IP 차단 등)을 URL 텍스트만으로 분석시키면 환각(지어내기) 위험이 있어, `generate_json_from_youtube`로 Gemini가 영상을 직접 시청하고 분석하도록 변경(`core/gemini_client.py`, `core/ai_analyzer.py`) |
| 체결내역 종목별 집계 — 현재가·등락률 참고 표시 | `/portfolio/trades` 종목별 집계 표에 단가 대비 현재가·등락률 컬럼 추가. 보유 종목은 `/portfolio/stocks` 일괄 조회, 청산된 종목은 `ensureStock`으로 개별 보완(순수 참고용, 평가손익은 계산하지 않음) |
| 캐치업 대상 종목 스코프 축소 | 위 현재가 표시 기능이 청산된 종목도 `Stock.is_active=True`로 영구 등록시키는 부작용이 있어, 캐치업(`catchup_price_history`/`catchup_portfolio_snapshots`)이 "보유 중이거나 실제 매매 이력이 있는 종목"만 대상으로 하도록 제한(`_tracked_stocks`) — 조회용 종목이 캐치업 스캔 대상으로 계속 불어나는 것을 방지. 캐치업에 30분 쿨다운도 추가해 로컬 재시작이 잦아도 매번 전 종목 재스캔하지 않도록 함(수동 버튼은 쿨다운 무시) |

## 🔶 진행 중 (워킹트리, 미커밋)

| 영역 | 상태 | 남은 것 |
|---|---|---|
| 오픈뱅킹(금융결제원) 연동 | OAuth 클라이언트·라우터·잔액 동기화 구현 완료(`openbanking_client.py`, `routes_openbanking.py`, `broker_deposit_sync.py`) | 테스트 환경에서 `3000103` 오류로 인증 화면 진입 불가 — 포털에 신청한 서비스명(오픈뱅킹 vs 어카운트인포) 확인 필요 |
| 레버리지 계산기 | `finance/leverage-calc` 신규 페이지 추가 | 사용자 검증 대기 |
| 자산 경고 · 고정비 유틸 | `asset-warnings.ts`, `fixed-expense-utils.ts`, `payment-coverage.test.ts` 추가 | - |
| SK하이닉스 ADR 티커 전환 | 2026-07-10 when-issued `SKHYV`로 등록 | 2026-07-13 정규 거래 시작 시 `SKHY`로 티커 변경 필요 (설정 화면에서 삭제 후 재등록, 티커는 수정 불가) |

## ⬜ 알려진 이슈

| 영역 | 내용 |
|---|---|
| 체결 날짜가 실제와 다를 수 있음 | KIS 체결내역의 `traded_at`을 "주문일자(ord_dt)"로 계산 — 예약매수/매도처럼 주문일과 실제 체결일이 다른 거래는 날짜가 어긋날 수 있음. 구체 사례 확인 중 |
| 삼성전자 재고 -2주 잔여 오차 | NXT 체결 복구(2024~2026 재동기화) 후에도 순매수 963주 vs 실제잔고 961주로 2주 차이 남음 — 2024년 이전 구간이거나 배당·단수주 등 비매매 조정으로 추정, 영향 미미해 보류 |
| 급등락 알림 과거 중복 미정리 | 재발 방지 로직은 수정됐지만, 이미 쌓인 `alert_history`의 과거 중복 행(같은 종목·같은 상황이 하루에도 여러 건)은 그대로 남아있음 — 원하면 정리 가능, 아직 안 함 |
| 매크로/섹터 감성 데이터 6/2~7/11 공백 | 프롬프트 버그로 이 구간 macro_signals는 0건, sector_signals는 감성 기본값(NEUTRAL)만 쌓임 — 백필하지 않기로 결정(비용 발생하는 재분석 생략), 앞으로 들어오는 데이터부터 정상 |
| PriceHistory/PortfolioSnapshot에 평일인데 값이 중복된 행 의심 (6/2·6/3) | 6/2(화)·6/3(수) 종가·스냅샷 값이 완전히 동일 — 주말 날짜가 아니라서 이번 정리(2026-07-24) 대상에서는 제외됨. 실제 두 날의 등락이 우연히 같았는지, 예전 wall-clock 버그의 또 다른 변종인지 미확인. 영향 적어 보류 |

---

**갱신 규칙**: 큰 기능이 완료/착수될 때 이 문서의 해당 행을 옮기고 기준일을 갱신한다. 세부 이력은 git log가 담당하고, 이 문서는 "현재 스냅샷"만 유지한다.
